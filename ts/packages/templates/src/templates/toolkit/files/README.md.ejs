# <%= projectName %>

Frontend toolkit library template.

## Scripts
```bash
<%= packageManager %> run build
<%= packageManager %> run test
<%= packageManager %> run lint
<%= packageManager %> run changelog
<%= packageManager %> run release:plan
```
