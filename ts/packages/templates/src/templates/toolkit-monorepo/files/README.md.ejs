# <%= projectName %>

Frontend toolkit monorepo template.

## Workspace Layout
```text
packages/
  core/
scripts/
```

## Scripts
```bash
<%= packageManager %> run build
<%= packageManager %> run test
<%= packageManager %> run lint
<%= packageManager %> run changeset
<%= packageManager %> run release:plan
```
