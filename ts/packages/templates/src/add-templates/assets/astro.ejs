<%
    function toLang(processor) {
        const map = {
            css: '',
            sass: 'scss',
            stylus: 'styl',
            less: 'less',
        };
        return map[processor] || '';
    }
    const styleLang = toLang(cssProcessor);
%>
---
    // frontmatter 逻辑放这里
---

<div></div>

<style<% if (styleLang) { %> lang="<%= styleLang %>"<% } %>>
</style>
